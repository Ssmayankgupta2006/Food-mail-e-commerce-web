var product_id =
  "I" +
  Math.round(Math.random() * 10000) +
  "am" +
  Math.round(Math.random() * 10000) +
  "Big";
document.querySelector(".productid").value = product_id;
document.querySelector(".productid").style.color = "red";
var product_search_id =
  "Search" +
  Math.round(Math.random() * 10000) +
  "Id" +
  Math.round(Math.random() * 10000) +
  "Product";
document.querySelector(".product_search_id").value = product_search_id;
document.querySelector(".product_search_id").style.color = "red";
